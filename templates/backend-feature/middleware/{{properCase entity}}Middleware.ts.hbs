import type { NextFunction, Request, Response } from "express";

const {{camelCase entity}}Middleware = async (
    _request: Request,
    _response: Response,
    next: NextFunction,
): Promise<void> => {
    next();
};

export { {{camelCase entity}}Middleware };
